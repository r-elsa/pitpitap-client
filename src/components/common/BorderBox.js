import { space, width, height, color } from 'styled-system';
import styled from 'styled-components';

export default styled.div`
    display: flex;
    justify-content: center;
    border-radius: 4rem;
    border: 1px solid #0255a4;
    background-color: #ffffff;
    ${space}
    ${width}
    ${height}
    ${color}
`
