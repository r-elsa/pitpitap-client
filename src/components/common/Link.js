import { space, color, textStyle, colorStyle, fontSize } from 'styled-system';
import styled from 'styled-components';

export default styled.p`
    text-decoration: underline;
    ${space}
    ${color}
    ${textStyle}
    ${colorStyle}
    ${fontSize}
`