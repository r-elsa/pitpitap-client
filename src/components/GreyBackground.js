import {
  space,
  layout,
  flexbox,
  colorStyle,
  color,
  border,
  shadow
} from "styled-system";
import styled from "styled-components";

export default styled.div`
  display: block;
  height: 100vh;
  width: 100vw;
  background-color: rgba(65, 77, 94, 0.8);
  left: 0;
  top: 0;
  position: fixed;
`;
